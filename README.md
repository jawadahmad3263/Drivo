# Backend for Drivo

This express project handles backend for a mobile app that is used by Drivo to manage their ride bookings ride scheduling etc. MongoDb is used for database in the app for best performance.

## Features

## Setup

3. Install dependencies:
   yarn
1. Clone the repository or Extract the Zip File:

    ```
    git@github.com:jawadahmad3263/Drivo.git
    ```

1. Install dependencies:

    ```
    cd Drivo
    yarn
    ```

1. Configure environment variables:

    - Make sure to setup environment variables whose example has been provided in the .env.example

1. Run the project:
    ```
    yarn start
    ```